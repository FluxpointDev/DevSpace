@inherits EditorBase

<RadzenLabel Text="Markdown" class="rz-my-0"></RadzenLabel>
<RadzenTextArea @bind-Value="@Bindvalue" Style="width: 100%; min-width: 100%; max-width: 100%; min-height: 200px;"
                oninput="event.target.style.height = Math.max(event.target.clientHeight, event.target.scrollHeight) + 'px';"
                Change="@((t) => InputChanged(t))" aria-label="Markdown TextArea" />
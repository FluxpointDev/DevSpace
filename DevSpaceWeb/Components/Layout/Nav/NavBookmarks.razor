<RadzenButton id="buttonBookmarks" @ref="@BookmarksButton" Icon="bookmark" IconColor="var(--rz-header-color)" Click="@(() => BoomarksPopup.ToggleAsync(BookmarksButton.Element))" ButtonStyle="ButtonStyle.Base" Variant="Variant.Text">
</RadzenButton>

<Popup id="popupBookmarks" @ref="@BoomarksPopup" class="rz-popup" Lazy="true" AutoFocusFirstElement="false">
    <RadzenStack Orientation="Orientation.Horizontal" JustifyContent="JustifyContent.SpaceBetween">
        <RadzenText TextStyle="TextStyle.Body1" Style="color: var(--rz-text-tertiary-color); font-weight: 600; margin-top: 8px; margin-left: 4px; margin-bottom: 2px;">Bookmarks</RadzenText>
    </RadzenStack>
</Popup>

<style>
    #popupBookmarks {
        width: 300px;
        margin-top: 2px;
        left: calc(100vw - 310px) !important;
    }
</style>

@code {
    [CascadingParameter]
    private SessionProvider Session { get; set; }

    public Popup BoomarksPopup { get; set; }
    public RadzenButton BookmarksButton { get; set; }
}
